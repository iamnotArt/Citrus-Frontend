package com.example.citrusapp.Components

import androidx.compose.ui.Modifier

//Box(modifier = Modifier.fillMaxSize()) {
    //Image(
        //modifier = Modifier
            //.wrapContentSize(unbounded = true)
            //.graphicsLayer {
                //val offset = pagerState.getOffsetFractionForPage(0)
                //translationX = (size.width / 2f) - (offset * size.width / 2)
                //translationY = size.height / 8f
            //},
        //painter = painterResource(id = R.drawable.blob),
        //contentDescription = null,
        //contentScale = FixedScale(2.5f),
        //colorFilter = ColorFilter.tint(TwentyFourBabyTheme.colors.primaryVariant)
    //)
//}



